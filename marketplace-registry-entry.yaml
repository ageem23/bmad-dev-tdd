# DRAFT registry entry for the BMad plugins marketplace.
#
# This file is not read by anything in this repo. It is the payload for a
# pull request against:
#
#   https://github.com/bmad-code-org/bmad-plugins-marketplace
#   -> registry/community/bmad-dev-tdd.yaml
#
# Before submitting:
#   1. Make this repository public.
#   2. Tag a release (git tag v0.1.0 && git push origin v0.1.0).
#   3. Fill approved_tag / approved_sha below from that tag.
#   4. Leave approved_date, reviewer, and trust_tier for the reviewers.
#
# Category is chosen from `categories.yaml` in the marketplace repo.
# software-development/dev-tools, not .../testing: this workflow's output is
# an implemented story, and it substitutes for bmad-dev-story in the story
# cycle. The QA slot is already held by bmad-qa-generate-e2e-tests, which
# generates tests for code that already exists. Tests are this module's
# mechanism, not its deliverable.

name: bmad-dev-tdd
display_name: Dev TDD (BMad Community Module)
code: bmad-tdd
description: Strict test-first story implementation — failure-mode analysis per behavior, then red-green-refactor, then defensive hardening justified by tests
repository: https://github.com/ageem23/bmad-dev-tdd
module_definition: src/module.yaml
npm_package: null
author: ageem23
license: MIT
default_selected: false
type: community
category: software-development
subcategory: dev-tools
keywords:
  - bmad
  - tdd
  - test-driven-development
  - story-implementation
  - development-workflow
  - defensive-programming

version: '0.1.0'
approved_tag: v0.1.0
approved_sha: null # TODO: pin to the tagged commit SHA
approved_date: null # set by reviewers
reviewer: null # set by reviewers
trust_tier: unverified
